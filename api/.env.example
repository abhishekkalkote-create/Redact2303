ENV=local
DATABASE_URL=postgresql+asyncpg://redactproof:redactproof@localhost:5432/redactproof

AWS_REGION=us-east-1

# Leave unset until the Cognito user pool exists (see setup instructions).
COGNITO_USER_POOL_ID=
COGNITO_APP_CLIENT_ID=
COGNITO_REGION=us-east-1

DEV_AUTH_ENABLED=true
DEV_AUTH_SECRET=dev-only-insecure-secret-change-me

CORS_ORIGINS=["http://localhost:3000"]
